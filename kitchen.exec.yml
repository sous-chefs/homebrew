---
driver:
  name: exec

transport:
  name: exec

provisioner:
  product_name: cinc
  chef_omnibus_install: false
  require_chef_omnibus: false
  chef_client_path: /opt/cinc-workstation/bin/cinc-client
  sudo: true

platforms:
  - name: macos-latest
